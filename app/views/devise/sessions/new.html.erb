<div class="min-h-screen flex items-start justify-center bg-gray-900 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full bg-gray-800 p-8 rounded-2xl shadow-lg">
    <h2 class="text-3xl font-extrabold text-center text-white mb-6">Log In</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "space-y-6" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div>
        <%= f.label :email, class: "block text-sm font-medium text-gray-300 mb-1" %>
        <%= f.email_field :email, class: "w-full px-4 py-2 bg-gray-900 border border-gray-600 rounded-md text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500", autofocus: true, autocomplete: "email" %>
      </div>

      <div>
        <%= f.label :password, class: "block text-sm font-medium text-gray-300 mb-1" %>
        <%= f.password_field :password, class: "w-full px-4 py-2 bg-gray-900 border border-gray-600 rounded-md text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500", autocomplete: "current-password" %>
      </div>

      <div>
        <%= f.submit "Log In", class: "w-full py-2 px-4 bg-indigo-500 hover:bg-indigo-600 text-white font-semibold rounded-md shadow-md focus:outline-none focus:ring-2 focus:ring-indigo-400 transition duration-150 ease-in-out" %>
      </div>
    <% end %>

    <div class="mt-4 text-center text-sm text-gray-400">
      <%#= render "devise/shared/links" %>
    </div>
  </div>
</div>
